in gcli

self._defaults
	lrms_finished_file
	lrms_jobid_file
	job_folder_location
	joblist_file
	joblist_lock

def __init__(self, Default, Resource[])

def check_authentication(self,[Gcli.SMSCG_AUTHENTICATION,Gcli.SSH_AUTHENTICATION]):
	# Return True,False
	# Exception: Unknown resource type

def enable_authentication(self,[Gcli.SMSCG_AUTHENTICATION,Gcli.SSH_AUTHENTICATION]):
	# Return True,False
	# Exception: Unknown resource type

def gsub(self, Application):
	# Return Job
	# Exceptions:
		Exception('invalid input-file argument')
		Exception('no available LRMS found')
		Exception from Scheduler.do_brokering
		Exception from utils.create_job_unique_token
		Exception from LRMS.submit_job

def gstat(self):
	# Return Job[]
	# Exceptions:
		Exception from _get_list_running_jobs

def gstat(self, Job):
	# Return Job
	# Exceptions:
		Exception('Unknown resource type')
		Exception from LRMS.__init__
		Exception('Failed finding resource associated to job')
		Exception from _lrms.check_status(job_obj)

def _get_list_running_jobs_filesystem(self):
	# Expect to read a file containing the Job information in the following form:
		"key_1"\t"value_1"
		"key_2"\t"value_2"
	Example:
		lrmsjobid	gsiftp://ocikbpra.unizh.ch:2811/....





Resource
    int max_cores_per_node
    int max_walltime
    int max_memory_per_node
    int queued
    int running
    int max_user_queue
    int max_user_run
    int user_queued
    int user_run
    string frontend
    string name
    int total_cores
    int max_quota
    int used_quota
    int type
    
    


#RFR (Request For Review)

	
